@model WebApplication5.Models.ExamViewModel

@{
    ViewBag.Title = "addExam";
    Layout = "~/Views/Shared/_admin_layout.cshtml";
}









<div class="content-page">

    <!-- Start content -->
    <div class="content">

        <div class="container-fluid">

            <div class="row">
                <div class="col-xl-12">
                    <div class="breadcrumb-holder">
                        <h1 class="main-title float-left">Add Exam</h1>
                        <ol class="breadcrumb float-right">
                            <li class="breadcrumb-item">Home</li>
                            <li class="breadcrumb-item active">Add Exam</li>
                        </ol>
                        <div class="clearfix"></div>
                    </div>
                </div>
            </div>
            <!-- end row -->



            <form method="post" action="~/Admin/addExam" enctype="multipart/form-data">
                @Html.AntiForgeryToken()
                <div class="form-horizontal">
                    <hr />
                    @Html.ValidationSummary(true, "", new { @class = "text-danger"  })
                    <div class="form-group">
                        <label class="ml-2" for="Name">Exam Title</label>
                        <div class="col-md-10">
                            @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "form-control", required = "required" } })
                            @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })
                        </div>
                    </div>

                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="file" class="" name="Image" required />
                        </div>
                    </div>

                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" value="Add Exam" class="btn btn-info"/>
                        </div>
                    </div>
                </div>
            </form>

            <div class="mt-3">
                <table class="table table-info table-striped">
                    <thead>
                        <th>Id</th>
                        <th>Name</th>
                        <th>Action</th>
                    </thead>
                    <tbody>

                        @foreach (var i in ViewData["exams"] as List<Exam>)
                        {
                            <tr>
                                <td>@i.Id</td>
                                <td>@i.Name</td>
                                <td><a href="~/Admin/deleteExam/@i.Id">Delete</a></td>
                            </tr>
                        }

                    </tbody>
                </table>
            </div>

            






        </div>
        <!-- end row -->







    </div>
    



</div>

		</div>


































